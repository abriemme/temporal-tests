{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r \".tool_input.file_path // empty\"); case \"$f\" in *.py) cd \"${CLAUDE_PROJECT_DIR:-.}\" || exit 0; out=$(uv run ruff check \"$f\" 2>&1 && uv run ruff format --check \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; } ;; esac",
            "statusMessage": "ruff check + format"
          }
        ]
      }
    ]
  }
}
